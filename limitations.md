# Limitations

Condition inference is limited by ambiguity or oddity in natural language.
For example,

- '/' is used ambiguously. Sometimes it means 'and' and
sometimes it means 'or'.
- 'and' can mean 'logical and', but it can also mean 'union' of two sets,
  which needs to get converted to 'or'

